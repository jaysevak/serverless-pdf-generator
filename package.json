{
  "name": "serverless-pdf-generator",
  "version": "1.0.0",
  "description": "Serverless functions for form processing and PDF generation",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.1048.0",
    "pdfkit": "^0.13.0"
  }
}